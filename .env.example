# Copy to .env and adjust. If DATABASE_URL is unset, SQLite is used (dev).
# DATABASE_URL=postgres://USER:PASSWORD@HOST:5432/DBNAME
SECRET_KEY=change-me-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
